# Created by Zap installer
[ -f "${XDG_DATA_HOME:-$HOME/.local/share}/zap/zap.zsh" ] && source "${XDG_DATA_HOME:-$HOME/.local/share}/zap/zap.zsh"
plug "zsh-users/zsh-autosuggestions"
plug "zap-zsh/supercharge"
plug "zap-zsh/zap-prompt"
plug "zsh-users/zsh-syntax-highlighting"
plug "sdiebolt/zsh-ssh-agent"

# My key bindings
. "${XDG_CONFIG_HOME:-$HOME/.config}/zshrc.d/key-bindings"

# My paths
. "${XDG_CONFIG_HOME:-$HOME/.config}/zshrc.d/paths"

# My environment variables
. "${XDG_CONFIG_HOME:-$HOME/.config}/zshrc.d/environment-variables"

# My SSH agent
. "${XDG_CONFIG_HOME:-$HOME/.config}/zshrc.d/ssh-agent-setup"
